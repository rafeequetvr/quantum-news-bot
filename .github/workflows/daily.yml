name: Quantum News Daily

on:
  schedule:
    - cron: '30 2 * * *' # ദിവസവും രാവിലെ 8 AM (IST)
  workflow_dispatch: # നേരിട്ട് റൺ ചെയ്യാൻ ഇത് സഹായിക്കും

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run script
        env:
          TELEGRAM_TOKEN: ${{8579546992:AAHumr2OPn9DyRamkqUTIJ8RPaKyX32Nu6Q}}
          GEMINI_API_KEY: ${{AIzaSyAd65Dgqtn4tilMwWR-9pYu3NgwnuOTF40}}
          CHAT_ID: ${{1328852027}}
        run: python main.py
